services:
  api:
    build: .
    container_name: notificationcenter
    ports:
      - "3000:3000"
    env_file:
      - .env
    extra_hosts:
      - "host.docker.internal:host-gateway"
    platform: linux/amd64